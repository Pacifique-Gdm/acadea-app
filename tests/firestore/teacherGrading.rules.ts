import{readFileSync}from"node:fs";import{assertFails,initializeTestEnvironment,type RulesTestEnvironment}from"@firebase/rules-unit-testing";import{doc,getDoc,setDoc,updateDoc,deleteDoc}from"@firebase/firestore";import{afterAll,beforeAll,describe,it}from"vitest";
let env:RulesTestEnvironment;beforeAll(async()=>{env=await initializeTestEnvironment({projectId:"demo-teacher-grading",firestore:{rules:readFileSync("firestore.rules","utf8")}})},30000);afterAll(async()=>env?.cleanup(),30000);const db=()=>env.authenticatedContext("teacher-a",{role:"teacher",schoolId:"school-a"}).firestore();
describe("cotations exclusivement via serveur",()=>{for(const collection of["courseGradingConfigs","gradeEntries"]){it(`refuse toute lecture/écriture directe de ${collection}`,async()=>{const ref=doc(db(),collection,"x");await assertFails(getDoc(ref));await assertFails(setDoc(ref,{schoolId:"school-a",schoolYearId:"year-a"}));await assertFails(updateDoc(ref,{score:1}));await assertFails(deleteDoc(ref))})}});
