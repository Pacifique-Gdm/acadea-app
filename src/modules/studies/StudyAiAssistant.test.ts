import{describe,expect,it}from"vitest";import fs from"node:fs";
describe("Study AI UI",()=>{const source=fs.readFileSync("src/modules/studies/StudyAiAssistant.tsx","utf8");it("reste dans Menu et propose les actions attendues",()=>{expect(source).toContain("Assistant IA — Direction des études");expect(source).toContain("Appliquer et créer un DRAFT");expect(source).toContain("Résume l’horaire actuel")});it("exige confirmation et n’appelle jamais publier",()=>{expect(source).toContain("requiresConfirmation");expect(source).not.toContain("publishTimetable")});it("affiche loading, erreurs, clarification et stabilité",()=>{for(const value of ["Analyse…","role=\"alert\"","NEEDS_CLARIFICATION","stabilityScore"])expect(source).toContain(value)})});
